{
  "name": "landscapearchive-org",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The Landscape Archive Foundation — international home of the open TLA-185/169 landscape metadata standard (landscapearchive.org).",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "smoke:org": "node scripts/smoke-org.mjs",
    "deploy": "node scripts/deploy-org-gated.mjs",
    "deploy:unsafe": "astro build && wrangler pages deploy dist --project-name landscapearchive-org --branch main --commit-dirty=true"
  },
  "dependencies": {
    "astro": "^5.6.1",
    "@astrojs/sitemap": "^3.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.103.0"
  }
}
